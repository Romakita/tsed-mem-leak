import { BeforeRoutesInit, Configuration, OnReady } from "@tsed/common";
import * as Express from "express";
import { SwaggerService } from "./services/SwaggerService";
export declare class SwaggerModule implements BeforeRoutesInit, OnReady {
    private swaggerService;
    private configuration;
    private expressApplication;
    private loaded;
    constructor(swaggerService: SwaggerService, configuration: Configuration, expressApplication: Express.Application);
    /**
     *
     */
    $beforeRoutesInit(): void;
    $onRoutesInit(): void;
    $onReady(): void;
    /**
     *
     * @param {ISwaggerSettings} conf
     * @param urls
     */
    private createRouter;
    private middlewareSwaggerJson;
    private mapSwaggerUIConfig;
    private middlewareRedirect;
    /**
     *
     * @returns {(req: any, res: any) => any}
     * @param conf
     * @param urls
     */
    private middlewareIndex;
    /**
     *
     * @param {e.Router} router
     * @param {string} path
     */
    private middlewareCss;
    /**
     *
     * @param {string} path
     */
    private middlewareJs;
}
