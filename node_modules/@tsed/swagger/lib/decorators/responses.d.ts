import { Response } from "swagger-schema-official";
/**
 *
 * @param {string | number} status
 * @param {Response} response
 * @returns {Function}
 * @decorator
 * @swagger
 */
export declare function Responses(status: string | number, response: Response): Function;
