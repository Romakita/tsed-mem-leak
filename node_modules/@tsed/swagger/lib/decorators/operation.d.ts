import { Operation as IOperation } from "swagger-schema-official";
/**
 *
 * @param {Operation | any} operation
 * @returns {Function}
 * @decorator
 * @swagger
 */
export declare function Operation(operation: IOperation | any): Function;
