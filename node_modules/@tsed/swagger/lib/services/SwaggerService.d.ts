import { Configuration, InjectorService, RouteService } from "@tsed/common";
import { Spec } from "swagger-schema-official";
import { ISwaggerSettings } from "../interfaces";
export declare class SwaggerService {
    private injectorService;
    private routeService;
    private configuration;
    constructor(injectorService: InjectorService, routeService: RouteService, configuration: Configuration);
    /**
     *
     * @returns {Spec}
     */
    getOpenAPISpec(conf: ISwaggerSettings): Spec;
    /**
     * Return the global api information.
     * @returns {Info}
     */
    getDefaultSpec(conf: Partial<ISwaggerSettings>): Spec;
    private readSpecPath;
    /**
     *
     * @param paths
     * @param definitions
     * @param ctrl
     * @param endpointUrl
     * @param getOperationId
     */
    private buildRoutes;
    /**
     *
     * @param ctrl
     */
    private buildTags;
    /**
     *
     * @param {ISwaggerSettings} conf
     * @returns {(targetName: string, methodName: string) => (any | string)}
     */
    private createOperationIdFormatter;
}
