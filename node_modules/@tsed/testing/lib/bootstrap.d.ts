/**
 * Load the server silently without listening port and configure it on test profile.
 * @decorator
 * @param server
 * @param args
 * @returns {Promise<void>}
 * @deprecated
 */
export declare function bootstrap(server: any, ...args: any[]): () => Promise<void>;
