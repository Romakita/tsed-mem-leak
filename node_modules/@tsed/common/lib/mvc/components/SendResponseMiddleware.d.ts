/// <reference types="express" />
import { ConverterService } from "../../converters";
import { Req, Res } from "../decorators";
import { IMiddleware } from "../interfaces/index";
/**
 * See example to override SendResponseMiddleware [here](/docs/middlewares/override/send-response.md).
 * @middleware
 */
export declare class SendResponseMiddleware implements IMiddleware {
    protected converterService: ConverterService;
    constructor(converterService: ConverterService);
    use(req: Req, response: Res): import("express").Response;
}
