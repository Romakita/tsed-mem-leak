import { InjectorService } from "@tsed/di";
import * as Express from "express";
/**
 * Bind request and create a new context to store information during the request lifecycle. See @@RequestContext@@ for more details.
 *
 * @param injector
 */
export declare function contextMiddleware(injector: InjectorService): (request: Express.Request<import("express-serve-static-core").ParamsDictionary>, response: Express.Response, next: Express.NextFunction) => Promise<void>;
